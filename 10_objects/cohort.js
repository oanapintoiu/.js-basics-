const cohort = {
    name: 'March 2023',
    id: 2023,
    peers: ['Arya', 'Q', 'Pablo', 'Oana']
};

console.log(cohort.id + ' - ' + cohort.name + ' cohort has ' + cohort.peers.length + ' students.');